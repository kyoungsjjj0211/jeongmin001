package com.thejoa703.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import com.thejoa703.entity.AppUser;

@Repository //★
public interface PostRepository extends JpaRepository <AppUser, Long> { //Entity , PK ★
}

/*
CREATE : sava - INSERT INTO appuser (컬럼1,컬럼2,,)  values (?,?,,)
READ : findAll - SELECT * from appuser 
       findById - SELECT * from appuser where id=?
UPDATE : save - update appuser seet 컬럼1=? , 컬럼2=? where id=?
DELETE : deleteById - delete from appuser
*/