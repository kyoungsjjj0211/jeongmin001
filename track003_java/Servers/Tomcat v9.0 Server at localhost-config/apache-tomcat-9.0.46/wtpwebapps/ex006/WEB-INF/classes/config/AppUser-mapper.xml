<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thejoa703.dao.AppUserDao">

<!-- <resultMaP id="AppuserMap" type="AppuserDto">
	<result property ="appUserId" coulumn="appUserId"/>
	<result property ="email" coulumn="email"/>
	<result property ="password" coulumn="password"/>
	<result property ="mbtiTypeId" coulumn="mbtiTypeId"/>	
</resultMaP> -->

	<insert id="insert" parameterType="AppUserDto">
	insert into appuser(APP_USER_ID, EMAIL, PASSWORD, MBTI_TYPE_ID)
 	values (appuser_seq.nextval ,#{email},#{password},#{mbtiTypeId})
	</insert>
	
	<update id="update" parameterType="AppUserDto">
	update appuser
	set password=#{password}, MBTI_TYPE_ID=#{mbtiTypeId}  
	where app_user_id=#{appUserId}
	</update>
	
	<delete id="delete" parameterType="AppUserDto">
	delete from appuser
	where app_user_id=#{appUserId} and password=#{password}
	</delete>
	
	
	<!-- <select resultType="appUserDto" id="selectAll">
	select * from appuser order by appUserId desc
	</select> -->
	
	<select resultType="AppuserMap" id="selectAll">
	select * from appuser order by APP_USER_ID desc
	</select>
	
	
	<!-- <select resultType="appUserDto" id="select" parameterType="int">
	    select * from appuser where appuserid=#{appUserId}
	</select> --> 
	
 	<select resultType="appUserMap" id="select" parameterType="AppUserDto">
	    select * from appuser where app_user_id=#{appUserId}
	</select>
	
	<select resultType="appUserMap" id="selectEmail" parameterType="AppUserDto">
	    select * from appuser where email=#{email}
	</select>
	
	<select resultType="int" id="selectLogin" parameterType="AppUserDto">
	    select * count(*) cnt from appuser where email=#{email} and password=#{password}
	</select>
	  
</mapper>